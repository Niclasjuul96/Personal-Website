# Todo / Roadmap

Architecture, current state, and past decisions moved to `CLAUDE.md` —
this file is now just the actual open todo list.

## Open

- Cleanup: old project image files from before the Storage migration are
  still sitting unused in Hostinger's File Manager
  (`public_html/assets/project-images/`) — deferred to the next time the
  site is actually redeployed to Hostinger (not done yet as of this
  session), do it alongside that upload rather than as a separate trip.
